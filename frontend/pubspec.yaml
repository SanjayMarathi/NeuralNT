name: neural_nt_frontend
description: A new Flutter project for NeuralNT.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  file_picker: ^8.1.7
  image_picker: ^1.0.0
  image: ^4.1.0
  connectivity_plus: ^5.0.0
  permission_handler: 12.0.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
